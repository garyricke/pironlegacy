# Secrets — never commit
.env
.env.*
*.key

# Dependencies (Netlify installs these at build time)
node_modules/

# Raster assets live on Cloudinary, never in the repo
generated_imgs/
assets/lead-donors/

# OS
.DS_Store

# Internal planning — keep out of the public repo
plan/

# Local Netlify build artifacts
.netlify/
deno.lock

# Scratch
scratchpad/
*.log
